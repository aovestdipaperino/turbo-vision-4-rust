1. ChDir dialog should match original borland appearance
2. FileDialog window not handling events properly
3. Fix dialogs appearance (too small, not aligned, not centered, drawn before the desktop) in few samples (full-demo, desklogo)
4. Option<Palette> -> Palette ?
